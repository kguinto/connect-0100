{
  "name": "connect-0100",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "babel client -w --out-dir client/compiled --presets=es2015,react --ignore=node_modules,client/compiled --source-maps inline",
    "serve": "nodemon server/server.js",
    "start": "npm run-script build & npm run-script serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1"
  },
  "presets": [
    "env"
  ]
}
